import { Text, View } from "react-native";

export default function index() {
  return (
    <View style={{ padding: 64, borderWidth: 8, borderColor: "green" }}>
      <Text>Index page (or default/home page) for Browse page for now</Text>
    </View>
  );
}
