name: Move new issues into Unassigned

on:
  issues:
    types: [opened]

jobs:
  automate-project-columns:
    runs-on: ubuntu-latest
    steps:
      - uses: alex-page/github-project-automation-plus@v0.8.3
        with:
          project: Badi
          column: Unassigned
          repo-token: ${{ secrets.GHPROJECT_TOKEN }}
